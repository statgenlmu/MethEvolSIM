seed: 123456

n_sim: 10000

# Absolute path (starts with /):
dir:
  /scratch/saracv/abc/cichlids
  #/home/sara/methylation/MethEvolSIM/extensive_tests/abc_inferences/test

# Relative paths:
methSite_genomicDist:
  reference_genome: genomic_dist/ncbi_dataset/ncbi_dataset/data/GCF_000238955.4/GCF_000238955.4_M_zebra_UMD2a_genomic.fna
  CGI_annotation: genomic_dist/CGI-Mzebra.txt
  CpG_count: genomic_dist/CpG_count.RData
  # /home/sara/methylation/MethEvolSIM/extensive_tests/abc_inferences/spatial_str.RData
  #/scratch/saracv/abc/cichlids/test1/spatial_str.RData
  
selected_region:
  name: dpp4
  chr: NC_036801.1
  start: 29502362
  end: 29502362
  side_bp: 500000 # to select structures starting after start - side_bp and ending before end + side_bp
  
  
tree: ((1:1,2:1):9,3:10);

n_tips: 3

prior_distributions:
  # Gamma distribution
  alpha_pI:
    shape: 3
    scale: 0.0333333333  # 1/30 as a decimal value
  # Gamma distribution
  beta_pI:
    shape: 3
    scale: 0.3333333333  # 1/3 as a decimal value
  # Gamma distribution
  alpha_pNI:
    shape: 3
    scale: 0.0333333333  # 1/30 as a decimal value
  # Gamma distribution
  beta_pNI:
    shape: 3
    scale: 0.3333333333  # 1/3 as a decimal value
  # Gamma distribution
  alpha_mI:
    shape: 3
    scale: 0.0333333333  # 1/30 as a decimal value
  # Gamma distribution
  beta_mI:
    shape: 3
    scale: 0.1666666667  # 1/6 as a decimal value
  # Gamma distribution
  alpha_mNI:
    shape: 3
    scale: 0.1666666667  # 1/6 as a decimal value
  # Gamma distribution
  beta_mNI:
    shape: 3
    scale: 0.0333333333  # 1/30 as a decimal value
  # Exponential distribution
  alpha_Ri:
    rate: 0.5  # 1/2 as a decimal value
  # Exponential distribution
  mu:
    rate: 50
  # Uniform distribution
  iota:
    min: 0
    max: 1
  # Uniform distribution
  tree_scale:
    min: 0.1
    max: 20

# Use CFTP algorithm at the root of the tree
CFTP: TRUE

# Number of cores for parallel simulations (rule run_sim)    
n_cores: 32


  



